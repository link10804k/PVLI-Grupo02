export default class ProductionMenu extends Phaser.GameObjects.Sprite {

constructor(scene, x, y, texture = "productionMenu", building) {
        super(scene, x , y, texture);

        this.building = building;
    }


    // AQUI SEGUIIIIIIIIIR
}